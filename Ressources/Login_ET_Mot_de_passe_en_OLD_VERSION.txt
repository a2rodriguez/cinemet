<?php $login = $_POST['login']; //recuperation les informations du formulaire d'identification 
$motdepasse = $_POST['motdepasse']; //recuperation les informations du formulaire d'identification 
mysql_connect("localhost","root",""); 
mysql_select_db("test");

$requete = "select login, motdepasse from user where login='$login' AND motdepasse ='$motdepasse' " ; 

$verif = mysql_query($requete) or die(mysql_error()); 
$row_verif = mysql_fetch_assoc($verif); 
$user = mysql_num_rows($verif); 
if($user) { 
echo "success"; 
} else 
{ 
echo "error"; 
} 
mysql_close(); ?> 